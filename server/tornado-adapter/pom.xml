<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.tornado</groupId>
        <artifactId>tornado-agent-server</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>tornado-adapter</artifactId>
    <name>tornado-adapter</name>
    <description>用户请求适配层：REST/SSE Controller、VO 组装、鉴权过滤器、全局异常处理</description>

    <dependencies>
        <dependency>
            <groupId>com.tornado</groupId>
            <artifactId>tornado-app</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.tornado</groupId>
            <artifactId>tornado-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.tornado</groupId>
            <artifactId>tornado-client</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.tornado</groupId>
            <artifactId>tornado-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
